\chapter{ROS2-Implementierung mit Python}

\begin{lernziele}
Nach diesem Kapitel verstehen Sie den Aufbau eines einfachen ROS2-Python-Nodes. Sie können Publisher, Subscriber, Timer und grundlegende Package-Strukturen einordnen und wissen, wie ein einfacher Node aus dem Modell abgeleitet werden kann.
\end{lernziele}

\section{Einleitung}
Nachdem die Architektur modelliert wurde, folgt die Umsetzung. In diesem Kapitel betrachten wir Python mit \texttt{rclpy}. Python eignet sich gut für den Einstieg, weil der Code vergleichsweise leicht lesbar ist.

\section{Package erstellen}
Ein Python-Package wird typischerweise mit folgendem Befehl erstellt:

\begin{lstlisting}[language=bash]
ros2 pkg create robot_basic_nodes --build-type ament_python --dependencies rclpy std_msgs sensor_msgs geometry_msgs
\end{lstlisting}

Der Parameter \texttt{--build-type ament\_python} legt den Build-Typ fest: \texttt{ament\_python} eignet sich für reine Python-Packages, während \texttt{ament\_cmake} für C++- oder gemischte Packages verwendet wird.

Ein Package bündelt Code, Konfiguration und Startinformationen. Für größere Systeme werden mehrere Packages sinnvoll. Abbildung~\ref{fig:kap17-workspace-struktur} zeigt die resultierende Workspace- und Package-Struktur.

\begin{figure}[H]
  \centering
  \resizebox{\textwidth}{!}{\input{figures/fig_kap17_workspace_struktur}}
  \caption{Verzeichnisstruktur eines ROS2-Workspace mit dem Package \texttt{robot\_basic\_nodes} sowie den Geschwisterordnern \texttt{build/}, \texttt{install/} und \texttt{log/}.}
  \label{fig:kap17-workspace-struktur}
\end{figure}

\section{Ein einfacher Node}
\begin{lstlisting}[language=Python]
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class StatusNode(Node):
    def __init__(self):
        super().__init__('status_node')
        self.publisher = self.create_publisher(String, '/robot/status', 10)
        self.timer = self.create_timer(1.0, self.publish_status)

    def publish_status(self):
        msg = String()
        msg.data = 'Robot is running'
        self.publisher.publish(msg)

def main():
    rclpy.init()
    node = StatusNode()
    rclpy.spin(node)
    node.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
\end{lstlisting}

Damit \texttt{ros2 run robot\_basic\_nodes status\_node} diesen Code später findet, muss zusätzlich ein passender Eintrag in den \texttt{entry\_points} der \texttt{setup.py} des Packages angelegt werden; darauf gehen wir hier nicht im Detail ein.

\section{Publisher}
Ein Publisher veröffentlicht Nachrichten auf einem Topic. Im Beispiel sendet der Node regelmäßig einen Status auf \texttt{/robot/status}. Ein Batteriemonitor würde ähnlich regelmäßig \texttt{/battery\_state} veröffentlichen.

\section{Subscriber}
Ein Subscriber empfängt Nachrichten von einem Topic. Der ObjectDetector würde zum Beispiel \texttt{/camera/image\_raw} abonnieren. Eine Callback-Funktion (eine Methode, die automatisch aufgerufen wird, sobald eine neue Nachricht eintrifft) verarbeitet eingehende Nachrichten.

\section{Timer}
Timer lösen Funktionen regelmäßig aus. Sie eignen sich für Statusmeldungen, simulierte Sensordaten oder periodische Prüfungen. Abbildung~\ref{fig:kap17-ablauf-publisher-timer} zeigt den Ablauf im \texttt{StatusNode} vom Timer bis zur Nachricht auf dem Topic.

\begin{figure}[H]
  \centering
  \input{figures/fig_kap17_ablauf_publisher_timer}
  \caption{Ablauf im \texttt{StatusNode}: Der Timer löst periodisch \texttt{publish\_status()} aus, das eine Nachricht erzeugt und auf dem Topic \texttt{/robot/status} veröffentlicht.}
  \label{fig:kap17-ablauf-publisher-timer}
\end{figure}

\section{Build und Start}
\texttt{colcon} ist das Standard-Build-Werkzeug für ROS2-Workspaces; es übersetzt und verlinkt alle Packages im Workspace gemeinsam. Der Workspace gliedert sich dabei typischerweise in die Ordner \texttt{src/} (Quellcode), \texttt{build/} und \texttt{install/} (Build-Ergebnisse) sowie \texttt{log/} (Protokolle), wie in Abbildung~\ref{fig:kap17-workspace-struktur} gezeigt.

\begin{lstlisting}[language=bash]
colcon build
source install/setup.bash
ros2 run robot_basic_nodes status_node
\end{lstlisting}

\section{Vom Modell zum Node}
Die \texttt{part def BatterySystem} kann zur Entscheidung führen, einen \texttt{battery\_monitor\_node} zu implementieren. Die Schnittstelle \enquote{Akkustatus} wird zum Topic \texttt{/battery\_state}. Der Testfall prüft später, ob der Node plausible Werte veröffentlicht.

\begin{hinweisbox}
Der Code ist bewusst minimal. In realen Projekten kommen Fehlerbehandlung, Parameter, Logging, Launch-Dateien, Tests und saubere Package-Strukturen hinzu.
\end{hinweisbox}

\section{Typische Fehler}
\subsection{Keine klare Verantwortung}
Ein Node sollte eine verständliche Aufgabe haben.

\subsection{Topic-Namen hart verteilen}
Topic-Namen sollten konsistent sein und bei Bedarf über Parameter konfigurierbar werden.

\subsection{Keine Fehlerbehandlung}
Auch einfache Nodes sollten ungültige Daten und Ausnahmen nicht ignorieren.

\section{Vom Minimalbeispiel zum Projektcode}
Das gezeigte Beispiel ist absichtlich klein. In einem echten Projekt sollte ein Node mehr können: Parameter lesen, Logging nutzen, ungültige Daten erkennen und sauber herunterfahren. Für den \texttt{battery\_monitor\_node} könnten Parameter definieren, ab welchem Prozentwert eine Warnung veröffentlicht wird.

Ein sinnvoller nächster Schritt ist ein simulierter Batterieknoten. Er veröffentlicht regelmäßig einen Akkustand, der langsam sinkt. Ein zweiter Node kann diesen Wert abonnieren und bei niedrigem Stand eine Warnung ausgeben. Damit entsteht ein kleines, testbares ROS2-System, das direkt zu Anforderungen und Modellbeziehungen passt.

Wichtig ist die Rückbindung an das Modell. Der Code sollte nicht isoliert entstehen. Jede Node sollte im Modell einen Zweck haben, einer Funktion zugeordnet sein und über definierte Topics kommunizieren.

\section{Zusammenfassung}
ROS2-Python-Nodes setzen Teile der modellierten Architektur um. Publisher, Subscriber und Timer bilden die Grundbausteine. Für den Einstieg genügt ein kleiner Node, solange seine Rolle im Modell klar ist.

\section{Kontrollfragen}
\begin{enumerate}
    \item Was ist ein ROS2-Package?
    \item Was macht ein Publisher?
    \item Was macht ein Subscriber?
    \item Wozu dient ein Timer?
    \item Warum sollte ein Node eine klare Verantwortung besitzen?
    \item Wie kann ein SysML-Systemelement (\texttt{part def}) zu einem ROS2-Node führen?
\end{enumerate}

\section{Übungen}
\begin{uebungbox}
\textbf{Übung 1: Battery Monitor}\\
Implementieren Sie konzeptionell einen \texttt{battery\_monitor\_node}, der regelmäßig einen simulierten Akkustand veröffentlicht.
\end{uebungbox}

\begin{uebungbox}
\textbf{Übung 2: Modellbezug}\\
Beschreiben Sie, welche Anforderung, welches Systemelement (\texttt{part def}), welches Topic und welcher Testfall zu Ihrem \texttt{battery\_monitor\_node} gehören.
\end{uebungbox}

\section{Literaturhinweise}
Für praktische ROS2-Implementierung ist die offizielle Dokumentation \cite{ros2docs} zentral. Das Kapitel bleibt bewusst ein Einstieg und bereitet das spätere vollständige ROS2-Projekt vor.
